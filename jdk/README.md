# JDKをインストール
 * Oracleのサイトからrpmをダウンロードしてサーバに転送する
 * rpm -ivh jdk-8uversion-linux-x64.rpm
 * JAVA_HOMEを定義し、PATHに$JAVA_HOME/binを追加する
 * export JAVA_HOME=/usr/java/jdk-xxx
 * export PATH=$JAVA_HOME/bin:$PATH

